spring:
  config:
    import: optional:classpath:common-lib-application.yaml
  application:
    name: user-training-interaction-service
  datasource:
    url: ${USER_TRAINING_INTERACTION_SERVICE_DB_URL:jdbc:postgresql://localhost:5432/user_training_interaction_service}
    username: ${USER_TRAINING_INTERACTION_SERVICE_DB_USERNAME:${DB_USERNAME:postgres}}
    password: ${USER_TRAINING_INTERACTION_SERVICE_DB_PASSWORD:${DB_PASSWORD:postgres}}
    driver-class-name: org.postgresql.Driver
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:fitness}
    password: ${RABBITMQ_PASSWORD:fitness}
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.xml

app:
  integration:
    training-service:
      base-url: ${TRAINING_SERVICE_BASE_URL:http://training-service:8080/api/v1}
